<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h2> <%= redbull.flavor %> </h2>
    <img src="<%= redbull.image.url %>" alt="" with="100" height="100">
    <h2> <%= redbull.withSuger%></h2>

   <%if(redbull.owner._id.equals(user._id)){ %>
    you own this!
<form action="/redbulls/<%= redbull._id %>?_method=Delete" method="post">
    <button type="submit">Delete</button>
</form>
     <a href="/redbulls/<%= redbull._id %>/edit"> edit </a>

 <% }%>
</body>
</html>